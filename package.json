{
  "name": "webdriver-promise-retry-example",
  "version": "1.0.0",
  "description": "A simple example showing how to retry tests in selenium webdriver js.",
  "main": "index.js",
  "scripts": {
     "test": "NODE_PATH=. mocha index.js"
  },
  "author": "ebinnion",
  "license": "ISC",
  "dependencies": {
    "mocha": "^3.4.2",
    "selenium-webdriver": "^3.5.0"
  }
}
